/- Compatibility entrypoint: current-paper-only proof ledger. -/

import AccessOrthogonality.CurrentPaperStatus

#eval s!"Access current-paper objects={AccessOrthogonality.CurrentPaper.Ledger.currentPaperEntries.length} derived={AccessOrthogonality.CurrentPaper.Ledger.currentPaperDerived.length} unfinished={AccessOrthogonality.CurrentPaper.Ledger.currentPaperUnfinished.length}"
